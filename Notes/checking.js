'use strict'


// It the grade is above 70, absences is less than 3. Return true
function canGraduate(grade,absences)
    if(grade > 70 && absence <=3) {
        return true;
    }

    // only accept invitation if they are both handome and rich, if not
    // return false
    function acceptInvitation (isHandsome, isRich){
        if(true == isHandsome && true == isRich){

        }

    }
// Why do we want to separate them?
// because we need to isolate the conditions to see if both are true
// rather than lumping them together. we need to determine if they stand true
// on their own

// This will come in handy during Tic-tac-toe
